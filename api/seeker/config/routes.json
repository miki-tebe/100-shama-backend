{
  "routes": [
    {
      "method": "GET",
      "path": "/seekers",
      "handler": "seeker.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/seekers/count",
      "handler": "seeker.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/seekers/:id",
      "handler": "seeker.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/seekers",
      "handler": "seeker.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/seekers/:id",
      "handler": "seeker.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/seekers/:id",
      "handler": "seeker.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
